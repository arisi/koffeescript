#!/usr/bin/env koffeescript.coffee
#encoding: UTF-8

require "pellejuttu"
require "cee.h"

int pelle =(int c) ->
  printf "tadaa %d\n",c

int main = (int argc,char **argv) ->
  if argc<2 or not true
    printf "EI ARGUMENNTTIA\n"
    exit(-1)
  int x=atoi(argv[1])
  int y=:pellekoodi
  for y=0;y<10;y++
    printf "LUUPPI %d\n", y
    if (y>4) and (y<9)
      printf "kohta loppuu luuppi\n"
  if x>10
    printf("iso\n") ;;;
    if x>100
      printf "TOSI ISO\n"
      if x>1000
        printf "HIMUTOSI ISO\n"
  else if x>5
    printf "keskikoko\n"
    printf "keskikoko juu!\n"
  else
    printf "pieni\n"  //KOMMENTTI
  pelle(:pellekoodi)
  printf "Required öä 0x%08X\n",pf()



